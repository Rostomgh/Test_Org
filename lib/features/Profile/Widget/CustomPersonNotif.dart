import 'package:flutter/material.dart';

class CustomPersonNotif extends StatelessWidget {
  const CustomPersonNotif({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}